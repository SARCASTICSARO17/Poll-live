# Dependency-related (frontend)
node_modules/

# Build output (frontend / backend)
frontend/dist/
backend/bin/
backend/tmp/

# Environment files — never commit real secrets
backend/.env
frontend/.env
*.env.local

# Logs
*.log

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Keep example env files tracked (no trailing .example in the above rules).
